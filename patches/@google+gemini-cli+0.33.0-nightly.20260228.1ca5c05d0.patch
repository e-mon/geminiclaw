diff --git a/node_modules/@google/gemini-cli/dist/src/zed-integration/zedIntegration.js b/node_modules/@google/gemini-cli/dist/src/zed-integration/zedIntegration.js
index a2a92fe..1ac4b4a 100644
--- a/node_modules/@google/gemini-cli/dist/src/zed-integration/zedIntegration.js
+++ b/node_modules/@google/gemini-cli/dist/src/zed-integration/zedIntegration.js
@@ -368,6 +368,7 @@ export class Session {
         const chat = this.chat;
         const parts = await this.#resolvePrompt(params.prompt, pendingSend.signal);
         let nextMessage = { role: 'user', parts };
+        let _lastUsageMetadata;
         while (nextMessage !== null) {
             if (pendingSend.signal.aborted) {
                 chat.addHistory(nextMessage);
@@ -403,6 +404,9 @@ export class Session {
                             });
                         }
                     }
+                    if (resp.type === StreamEventType.CHUNK && resp.value.usageMetadata) {
+                        _lastUsageMetadata = resp.value.usageMetadata;
+                    }
                     if (resp.type === StreamEventType.CHUNK && resp.value.functionCalls) {
                         functionCalls.push(...resp.value.functionCalls);
                     }
@@ -430,7 +434,7 @@ export class Session {
                 nextMessage = { role: 'user', parts: toolResponseParts };
             }
         }
-        return { stopReason: 'end_turn' };
+        return { stopReason: 'end_turn', _meta: { usageMetadata: _lastUsageMetadata } };
     }
     async sendUpdate(update) {
         const params = {
