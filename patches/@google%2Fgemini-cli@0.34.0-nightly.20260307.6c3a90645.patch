diff --git a/bundle/gemini.js b/bundle/gemini.js
index ea0166058a9bacdd0311e085cb112df5d6828786..72bc2eef5d0e9879cded610e9615984904a1b9e7 100755
--- a/bundle/gemini.js
+++ b/bundle/gemini.js
@@ -424765,7 +424765,7 @@ var init_local_executor = __esm({
         }, message.parts || [], promptId, signal, role);
         const functionCalls = [];
         let textResponse = "";
-        for await (const resp of responseStream) {
+        var _lastUsageMetadata, _lastModelVersion; for await (const resp of responseStream) {
           if (signal.aborted)
             break;
           if (resp.type === StreamEventType.CHUNK) {
@@ -570928,7 +570928,7 @@ var GeminiAgent = class {
       this.settings
     );
     this.sessions.set(sessionId2, session);
-    session.streamHistory(sessionData.messages);
+    // [geminiclaw patch] streamHistory() removed — see patch 4
     setTimeout(() => {
       session.sendAvailableCommands();
     }, 0);
@@ -571168,7 +571168,7 @@ ${thought.description}`;
     if (commandText && (commandText.startsWith("/") || commandText.startsWith("$"))) {
       const handled = await this.handleCommand(commandText, parts2);
       if (handled) {
-        return { stopReason: "end_turn" };
+        return { stopReason: "end_turn", _meta: { usageMetadata: _lastUsageMetadata, modelVersion: _lastModelVersion } };
       }
     }
     let nextMessage = { role: "user", parts: parts2 };
@@ -571191,7 +571191,7 @@ ${thought.description}`;
           LlmRole.MAIN
         );
         nextMessage = null;
-        for await (const resp of responseStream) {
+        var _lastUsageMetadata, _lastModelVersion; for await (const resp of responseStream) {
           if (pendingSend.signal.aborted) {
             return { stopReason: CoreToolCallStatus.Cancelled };
           }
@@ -571213,7 +571213,7 @@ ${thought.description}`;
           }
           if (resp.type === StreamEventType.CHUNK && resp.value.functionCalls) {
             functionCalls.push(...resp.value.functionCalls);
-          }
+          } if (resp.type === StreamEventType.CHUNK) { if (resp.value.usageMetadata) { _lastUsageMetadata = resp.value.usageMetadata; } if (resp.value.modelVersion) { _lastModelVersion = resp.value.modelVersion; } }
         }
         if (pendingSend.signal.aborted) {
           return { stopReason: CoreToolCallStatus.Cancelled };
@@ -571242,7 +571242,7 @@ ${thought.description}`;
         nextMessage = { role: "user", parts: toolResponseParts };
       }
     }
-    return { stopReason: "end_turn" };
+    return { stopReason: "end_turn", _meta: { usageMetadata: _lastUsageMetadata, modelVersion: _lastModelVersion } };
   }
   async handleCommand(commandText, parts2) {
     const gitService = await this.config.getGitService();
@@ -572558,7 +572558,7 @@ async function main() {
         process.exit(ExitCodes2.FATAL_AUTHENTICATION_ERROR);
       }
       let stdinData = "";
-      if (!process.stdin.isTTY) {
+      if (!process.stdin.isTTY && !argv.acp) {
         stdinData = await readStdin();
       }
       const injectStdinIntoArgs = (args2, stdinData2) => {
